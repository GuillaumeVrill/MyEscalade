<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Holo.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="topo_title_style">
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/btn_color1</item>
        <item name="android:textSize">@dimen/title_size</item>
        <item name="android:padding">@dimen/padding_size</item>
    </style>

    <style name="perf_title_style">
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@android:color/black</item>
        <item name="android:textSize">@dimen/title_size</item>
        <item name="android:padding">@dimen/padding_size</item>
    </style>

    <style name="topo_title2_style">
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@android:color/black</item>
        <item name="android:textSize">@dimen/normal_text</item>
        <item name="android:padding">@dimen/padding_size</item>
    </style>

    <style name="normal_text_style">
        <item name="android:textStyle">normal</item>
        <item name="android:textColor">@android:color/black</item>
        <item name="android:textSize">@dimen/normal_text</item>
        <item name="android:padding">@dimen/padding_size</item>
    </style>

</resources>
