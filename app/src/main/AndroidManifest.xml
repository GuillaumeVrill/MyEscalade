<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.guillaume.myescaladedemo" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".TopoguideActivity"
            android:label="@string/title_activity_topoguide" >
        </activity>
        <activity
            android:name=".PerformanceActivity"
            android:label="@string/title_activity_performance" >
        </activity>
        <activity
            android:name=".RegisterPerformanceActivity"
            android:label="@string/title_activity_register_performance" >
        </activity>
    </application>

</manifest>
